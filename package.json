{
  "name": "parallax-gate",
  "private": true,
  "version": "0.2.0",
  "description": "Solo Leveling-style career RPG with live Fitbit + LeetCode stat integration",
  "scripts": {
    "install:all": "npm install && npm --prefix client install && npm --prefix server install",
    "dev": "concurrently -n \"SRV,CLI\" -c \"cyan,magenta\" \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
